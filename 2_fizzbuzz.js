// write this function instruction for my student to solve, use test case as question criteria, follow 1_palindrom.js format
// File: 2_fizzbuzz.js
/**
 * FizzBuzz function that returns an array of numbers or strings based on the FizzBuzz
 *  rules:
 * - If a number is divisible by 3, return "fizz".
 * - If a number is divisible by 5, return "buzz".
 * - If a number is divisible by both 3 and 5, return "fizzbuzz".
 * * @param {number} start - The starting number of the range (inclusive).
 * * @param {number} end - The ending number of the range (inclusive).
 * * @returns {Array} - An array containing numbers or "fizz", "buzz", or "fizzbuzz".
 * * Example:
 * * - `fizzbuzz(1, 5)` should return `[1, 2, 'fizz', 4, 'buzz']`
 * * - `fizzbuzz(3, 15)` should return `['fizz', 4, 'buzz', 'fizz', 7, 8, 'fizz', 'buzz', 11, 'fizz', 13, 14, 'fizzbuzz']`
 * * - `fizzbuzz(15, 15)` should return `['fizzbuzz']`
 * * - `fizzbuzz(1, 1)` should return `[1]`
 * * Edge cases:
 * * - If `start` equals `end`, return an array with that single number or "fizzbuzz" if it meets the criteria.
 * * - If the range includes negative numbers, handle them correctly.
 * * - If the range includes zero, treat it as "fizzbuzz" since 0 is divisible by both 3 and 5.
 */


const fizzbuzz = (start, end) => {
    
}

module.exports = { fizzbuzz };