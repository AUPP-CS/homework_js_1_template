// The task is to implement a function `isPalindrome` that checks if a given string is a palindrome.
// A palindrome is a string that reads the same backward as forward, ignoring spaces, punctuation,
// and case differences.
// For example:
// - `isPalindrome('racecar')` should return `true`
// - `isPalindrome('hello')` should return `false`
// - `isPalindrome('A man a plan a canal Panama')` should return `true`
// - `isPalindrome('Was it a car or a cat I saw')` should return `true`
// - `isPalindrome('Madam, I\'m Adam')` should return `true`


const isPalindrome = (str) => {
  
}

module.exports = { isPalindrome };